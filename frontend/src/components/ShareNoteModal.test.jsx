import { render, screen, fireEvent } from "@testing-library/react"; import ShareNoteModal from "./ShareNoteModal"; describe("ShareNoteModal", () => { const mockOnClose = jest.fn(); const mockOnShare = jest.fn(); const note = { title: "My Important Note", }; beforeEach(() => { jest.clearAllMocks(); }); test("renders the share note modal", () => { render( <ShareNoteModal note={note} onClose={mockOnClose} onShare={mockOnShare} /> ); expect(screen.getByText("Share note")).toBeInTheDocument(); expect( screen.getByRole("heading", { name: 'Share "My Important Note"', }) ).toBeInTheDocument(); expect( screen.getByLabelText("User's email address") ).toBeInTheDocument(); expect( screen.getByPlaceholderText("Enter email address") ).toBeInTheDocument(); expect( screen.getByRole("button", { name: "Cancel" }) ).toBeInTheDocument(); expect( screen.getByRole("button", { name: "Share Note" }) ).toBeInTheDocument(); }); test("updates the email input when user types", () => { render( <ShareNoteModal note={note} onClose={mockOnClose} onShare={mockOnShare} /> ); const emailInput = screen.getByLabelText("User's email address"); fireEvent.change(emailInput, { target: { value: "test@example.com" }, }); expect(emailInput).toHaveValue("test@example.com"); }); test("calls onShare with trimmed email when Share Note is submitted", () => { render( <ShareNoteModal note={note} onClose={mockOnClose} onShare={mockOnShare} /> ); const emailInput = screen.getByLabelText("User's email address"); fireEvent.change(emailInput, { target: { value: " test@example.com " }, }); fireEvent.click( screen.getByRole("button", { name: "Share Note" }) ); expect(mockOnShare).toHaveBeenCalledTimes(1); expect(mockOnShare).toHaveBeenCalledWith("test@example.com"); }); test("does not call onShare when email is empty", () => { render( <ShareNoteModal note={note} onClose={mockOnClose} onShare={mockOnShare} /> ); const form = screen.getByRole("button", { name: "Share Note", }).closest("form"); fireEvent.submit(form); expect(mockOnShare).not.toHaveBeenCalled(); }); test("calls onClose when Cancel is clicked", () => { render( <ShareNoteModal note={note} onClose={mockOnClose} onShare={mockOnShare} /> ); fireEvent.click( screen.getByRole("button", { name: "Cancel" }) ); expect(mockOnClose).toHaveBeenCalledTimes(1); }); test("calls onClose when the close button is clicked", () => { render( <ShareNoteModal note={note} onClose={mockOnClose} onShare={mockOnShare} /> ); const closeButton = screen.getByRole("button", { name: "×" }); fireEvent.click(closeButton); expect(mockOnClose).toHaveBeenCalledTimes(1); }); test("calls onClose when the modal overlay is clicked", () => { render( <ShareNoteModal note={note} onClose={mockOnClose} onShare={mockOnShare} /> ); const overlay = document.querySelector(".modal-overlay"); fireEvent.click(overlay); expect(mockOnClose).toHaveBeenCalledTimes(1); }); test("does not close when clicking inside the modal", () => { render( <ShareNoteModal note={note} onClose={mockOnClose} onShare={mockOnShare} /> ); const modal = document.querySelector(".share-modal"); fireEvent.click(modal); expect(mockOnClose).not.toHaveBeenCalled(); 

}); });